//const worker = new Worker(globalThis.vegvisir);

globalThis.vegvisir.Navigation = class Navigation {
	/**
	 * Create a new Vegvisir soft navigation
	 * @param {URL|string|null} href 
	 */
	constructor(href = null) {

	}
}